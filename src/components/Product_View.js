import React, {useState, useEffect} from 'react';
import "./style/product_view.css";

const [products, setProducts] = useState([]);
useEffect({});

const Product_View = (pro) => {
	return (
		<div className="container">

		</div>
	);
};

export default Product_View;
